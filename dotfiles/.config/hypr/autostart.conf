# exec-once = uwsm app -- dropbox-cli start
exec-once = uwsm app -- udiskie                             # Auto-mount drives
exec-once = uwsm app -- wl-clip-persist --clipboard regular # Keep clipboard after application close
exec-once = uwsm app -- clipse -listen                      # Clipboard manager
exec-once = uwsm app -- hyprsunset

# omarchy is supposed to be taking care of these, but sometimes they don't start
# correctly or stay started
# TODO: Is elephant still needed?
# exec-once = uwsm app -- elephant
exec-once = uwsm app -- walker --gapplication-service
